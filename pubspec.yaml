name: flexus_core
description: Flexus Core Library.
version: 3.3.24
homepage: https://dilexusit.com

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  #Dev
  logger: ^1.1.0
  get: ^4.6.3
  lifecycle: ^0.5.0

  #UI
  loading_overlay: ^0.3.0
  sizer: ^2.0.15
  device_preview: ^1.0.0
  device_preview_screenshot: ^1.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
